- hosts: localhost
  tasks: 
        - docker_container:
                        name: "newos1"
                        image: "centos"
                        state: started
                        restart: yes
                        interactive: yes
                        tty: yes
                       
                        

                                
                 
