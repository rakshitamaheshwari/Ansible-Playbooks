- hosts : localhost
  tasks: 
     - copy: 
        dest: /root/ansible.txt
        content: "{{ansible_enp0s3.ipv4.address}} {{ansible_hostname}} {{ansible_processor_count}} {{ansible_enp0s3.macaddress}}"
